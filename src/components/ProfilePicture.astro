---
import ProfilePictureImage from '../media/profile_picture.png';
import { Image } from 'astro:assets';

const { width, height } = Astro.props;
---

<Image
	width={width ?? 150}
	height={height ?? 150}
	class="rounded-full border-2 border-st-tropaz-700"
	src={ProfilePictureImage}
	loading="eager"
	alt=""
/>
