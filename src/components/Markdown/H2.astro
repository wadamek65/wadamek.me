---
const props = Astro.props;
---

<h2 {...props} class="relative">
	<a
		class="absolute -left-6 !text-puerto-rico-300 no-underline underline-offset-4 hover:!text-puerto-rico-200 hover:underline sm:-left-8"
		href={`#${props.id}`}>#</a
	>
	<slot />
</h2>
